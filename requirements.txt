# Shared analysis dependencies.
#
# The executed notebooks are the authoritative record of the validated model
# installation/version checks. Convenience manifests are provided here:
#
# TimesFM 2.5:
#   pip install -r requirements-timesfm.txt
#
# Moirai 2.0:
#   pip install -r requirements-moirai2.txt
#
# The validated TimesFM and Moirai model runs used different compatible
# environments/devices; inference-speed comparisons are therefore excluded.
-r requirements-common.txt
